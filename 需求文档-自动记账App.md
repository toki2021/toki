# 自动记账 App 需求文档（V1）

> 由 2026-08-28 需求问答整理而成。状态：待确认。

## 1. 产品定位

个人自用的 Android 自动记账 App。核心体验：**微信/支付宝的支付通知到达后，点一下通知确认入账**，账目分类自动完成，数据全部存在本地，支持 WebDAV 云备份防丢失。

- 使用对象：仅自己（不做应用商店上架，无隐私合规负担）
- 兼容范围：Android 13+（minSdk 33）
- 技术栈：Kotlin + Jetpack Compose + Room + Material 3（自动深色模式）

## 2. 核心业务链路

```
微信/支付宝支付通知
        │  NotificationListenerService 监听
        ▼
   解析金额 + 商户名
        │  弹出"待确认"通知（可操作按钮）
        ▼
 ┌─ 点【入账】→ 规则库自动分类 → 写入数据库
 ├─ 点【修改后入账】→ 打开编辑页（改金额/分类/商户）
 └─ 点【忽略】→ 丢弃
        ▼
   统计页（每日趋势 + 下钻明细）
```

### 2.1 通知监听（自动抓取）

- 使用系统 `NotificationListenerService`，**不做无障碍读屏**（二期也不考虑）。
- 需适配的来源：微信支付、支付宝的推送通知。
- 通知里通常只有金额 + 商户名；解析失败的原始通知存入"待处理"列表，供手动补录。
- 入账方式为**通知确认制**：自动入账不开启，每一笔都需确认。

### 2.2 CSV 账单导入（兜底）

- 支持导入微信 / 支付宝导出的账单 CSV。
- **去重策略：全部人工确认**。导入时将疑似重复（金额+日期接近、商户相似）的记录标出，由用户逐笔决定"跳过/仍要导入"；不做自动去重。
- 导入的记录来源标记为 CSV，与通知记录区分。

### 2.3 分类与规则库

- 内置一套默认分类（餐饮、交通、购物、居住、娱乐、医疗、其他……）。
- **自动分类 = 可编辑规则库**：规则为「商户关键词 → 分类」，按优先级匹配；未命中归入"其他"。
- 规则可在 App 内增删改、调优先级。
- **常用商户快速选择**：通知里只有金额时，确认/编辑页显示历史商户标签一键补填。

### 2.4 读屏补全商户（无障碍，V1.5 已实现）

- 用户打开微信/支付宝账单详情页时，`BillReaderService` 自动读取页面文本，按「页面特征 + 收款方/商家标签 + 相邻节点」提取商户。
- 补全逻辑：10 分钟内同金额的待确认条目 → 更新商户并重发确认通知；同金额、商户为泛称（微信支付/支付宝/未知商户）的通知入账 → 直接更新商户与分类；都没有 → 新建待确认条目。
- 仅读取微信/支付宝的窗口内容，不上传任何数据；设置页有开关与状态显示。

### 2.4 记录类型

| 类型 | 说明 |
|------|------|
| 支出 | 默认类型，来自通知确认 / CSV 导入 / 手动补录 |
| 退款冲销 | 微信/支付宝的退款通知同样可被监听确认；退款关联原支出进行冲销，避免支出虚高 |

第一版**不做**：收入记录、转账/垫付标记、信用类（花呗/信用卡）记账口径、多账本、多币种、预算。

## 3. 数据存储与备份

- **纯本地**：Room 数据库，无账号体系、无后端、无数据上报。
- **WebDAV 云备份**：用户在设置页自行填写 WebDAV 地址和凭据（如坚果云），App 支持手动/定时上传备份文件、从备份恢复。备份文件为整库导出的压缩档。

### 主要数据模型（草案）

- `Transaction`：id、类型（支出/退款）、金额、商户名、分类 id、时间、来源（通知/CSV/手动）、关联的退款目标 id、原始通知文本
- `Category`：id、名称、图标
- `Rule`：id、商户关键词、目标分类、优先级
- `PendingEntry`：待确认/解析失败的通知缓存
- `BackupConfig`：WebDAV 地址、账号、加密存储的凭据、备份计划

## 4. 页面清单（V1）

1. **首页**：今日/本月支出概览 + 最近账单列表（可删改）
2. **统计页**：每日消费趋势图；点击某天下钻到当天账单明细
3. **导入页**：选 CSV → 预览 → 重复记录人工确认列表 → 入库
4. **规则管理页**：规则列表的增删改、排序
5. **分类管理页**：分类增删改
6. **设置页**：通知监听权限引导、WebDAV 备份配置、待处理通知列表入口
7. **编辑页**：单笔账单的新增/修改（通知上的"修改后入账"也进这里）

## 5. 权限清单

| 权限 | 用途 | 时机 |
|------|------|------|
| `POST_NOTIFICATIONS` | 弹出"待确认入账"通知 | 首次启动引导 |
| 通知使用访问权（NotificationListenerService 绑定） | 监听微信/支付宝通知 | 首次启动引导，跳系统设置 |
| `INTERNET` | WebDAV 备份 | 静默声明 |

## 6. 分期计划

- **V1.0（核心闭环）**：通知监听 + 确认入账 + 手动增删改 + 分类/规则库 + 本地 Room 存储
- **V1.1**：每日趋势统计 + 下钻明细
- **V1.2**：CSV 导入 + 人工去重、退款冲销
- **V1.3**：WebDAV 云备份/恢复

（二期暂存池：月度分类占比、月度环比、预算提醒、收入记账、多设备同步。读屏补全商户已提前于 V1.5 实现。）
